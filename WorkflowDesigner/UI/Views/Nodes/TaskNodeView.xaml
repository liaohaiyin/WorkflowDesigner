<UserControl x:Class="WorkflowDesigner.UI.Views.Nodes.TaskNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="140" Height="80">
    <Border Background="#F3E5F5" BorderBrush="#9C27B0" BorderThickness="2" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5,3">
                <TextBlock Text="⚙" FontSize="14" Foreground="#9C27B0" Margin="0,0,3,0"/>
                <TextBlock Text="任务" FontWeight="Bold" FontSize="12" Foreground="#7B1FA2"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Text="{Binding NodeName}" 
                       FontSize="10" HorizontalAlignment="Center" 
                       VerticalAlignment="Center" TextWrapping="Wrap"
                       Margin="3" Foreground="#4A148C"/>

            <Rectangle Grid.Row="2" Height="3" Fill="{Binding StatusBrush}" Margin="3"/>
        </Grid>
    </Border>
</UserControl>